import React from "react";

const TaskList = ({ tasks, setTask }) => {
  return (
    <div>
      <h2>Tasks to do</h2>
      <ul>//task should be maped be list item passes index and set task</ul>
    </div>
  );
};

export default TaskList;
